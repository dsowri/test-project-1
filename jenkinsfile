pipeline {
    agent any

    stages {
        stage('SCM') {
            steps {
                script {
                    git 'https://github.com/dsowri/test-project-1.git'
                }
            }
        }

        stage('SonarQube analysis') {
            steps {
                script {
                    def scannerHome = tool 'sonarqube_scanner-1'
                    withSonarQubeEnv('sonarqube-1') {
                        sh "${scannerHome}/bin/sonar-scanner"
                    }
                }
            }
        }
    }
}
